services:
  - type: web
    name: meevo-backend
    env: node
    branch: main
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    rootDir: backend
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
